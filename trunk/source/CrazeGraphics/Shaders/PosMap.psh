struct PS_OUTPUT
{
	float4 pos : SV_Target0;
	float4 norm : SV_Target1;
};

PS_OUTPUT main(float3 wsPos : TEXCOORD0, float3 normal : NORMAL0)
{
	PS_OUTPUT output;
	output.pos = float4(wsPos, 1.f);
	output.norm = float4(normal, 1.f);

	return output;
}